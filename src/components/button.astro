---
import { cn } from "@/lib/utils";

const { className } = Astro.props;
---

<button
  class={cn(
    "px-4 py-2 border border-slate-600 bg-slate-600 dark:bg-[#1f2937] hover:opacity-80 text-white font-meidum text-base rounded-md flex items-center gap-1 w-fit duration-200",
    className
  )}
>
  <slot />
</button>
